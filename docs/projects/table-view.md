# Table view

The table view is the best way to have all your tasks in a project visually represented on a vertical grid. This view lets you easily and quickly **filter**, **sort** and **compare** tasks side-by-side by toggling the visibility of different columns.

<img :src="$withBase('/assets/img/projects/table-view.png')">